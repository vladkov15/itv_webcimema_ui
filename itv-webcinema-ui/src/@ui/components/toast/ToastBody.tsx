import createWithPrefix from '@ui//utils/createWithPrefix'

import styles from './Toast.module.scss'

const Body = createWithPrefix('toast-body', { className: styles['toast-body'] })

export default Body
